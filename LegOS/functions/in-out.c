#include "../include/in-out.h"


//you can do an IO operation on any unused port: the Linux kernel by default uses port 0x80, 
//which is often used during POST to log information on the motherboard's hex display but
//almost always unused after boot.

